#include <stdio.h>
#pragma warning(diable: 4996 6031)
int f(int a, int b, int c) {
	if (c == 0)return;
	printf("%d ", a);
	return f(a + b, b, c - 1);
}
int main() {
	int a, b, c;
	scanf("%d" "%d" "%d", &a, &b, &c);
	f(a, b ,c);
	return 0;
}
