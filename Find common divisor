#include <stdio.h>
#pragma warning(disable: 4996 6031)
int main() {
	int a;
	int b;
	int tmp;
	scanf("%d %d", &a, &b);
	if (a >= b) {
		tmp = b;
	}
	else {
		tmp = a;
	}
	for (int i = 1; i <= tmp; i++) {
		if (b % i == 0 && a % i == 0) {
			printf("%d ", i);
		}
	}
}
