#include <stdio.h>
#pragma warning(disable : 4996 6031)
int main() {
	int a;
	int flag = 0;
	scanf("%d", &a);
	for (int i = 2; i < a; i++) {
		if (a % i == 0) {
			flag = 1;
			break;
		}
	}
	if (flag == 1) {
		printf("소수아니다");
	}
	else {
		printf("소수이다");
	}
}
