#include <stdio.h>
#pragma warning(disable : 4996 6031)
int main() {
	int a;
	scanf("%d", a);
	int c = 1;
	
	while (1) {
		if (a % c == 0) {
			printf("%d ", c);
			a = a / c;
			if (a == 1) {
				break;
			}
		}
		else {
			c++;
		}
		
	}
	return 0;
}
