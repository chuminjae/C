# include <stdio.h>
#pragma warning(disable:4996)
int main() {
	int b = 0;
	int c = 0;
	int x = 0;
	scanf("%d", &x);
	for (int a = x; a <= 11; a++) {
		if (a % 2 == 0) {
			b++;
		}
		else { c++; }
	}
	printf(" Number of even number :%d, Number of odd number :%d", b, c);
	return 0;
}
